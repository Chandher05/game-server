var GamePlayListeners = (socket) => {


    // socket.on('sendUserId', async (authId) => {
    //     userId = socket.handshake.userUID
    //     if (userId) {
    //         console.log("User connected to server " + userId)
    //     } else {
    //         console.log("Invalid auth")
    //     }
    // })


    
}

export default GamePlayListeners